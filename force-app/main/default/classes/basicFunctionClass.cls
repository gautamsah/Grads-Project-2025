public with sharing class basicFunctionClass {
    public basicFunctionClass() {
        Integer a = 10;
        Integer b = 20;
        Integer c = a + b;
        System.debug('The sum is: ' + c);
    }
}